const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],audio=$('#audio'),story=$('#story'),lyricsEl=$('#lyrics'),bg=$('#bg');
let sourceBase=[],sourceLines=[],reviewLines=[],lines=[],selected=0,offset=0,audioFile=null,audioURL=null,bgURL=null,bgMedia=null,abort=false,manualIndex=0,selectedSong=null,artworkObjectURL=null,manualBgFile=null,reviewMode='timed',activeStep=0,visitedSteps=new Set([0]),autosaveTimer=null,restoring=false;
const MAX=600,SAVE_KEY='lina.project.v2',DB_NAME='lina-project-media',DB_STORE='files',STEPS=['setup','lyrics','style','background'],NL=String.fromCharCode(10),CR=String.fromCharCode(13);
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const smooth=x=>{x=clamp(x,0,1);return x*x*(3-2*x)};
const esc=v=>String(v??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
function status(t){if($('#topStatus'))$('#topStatus').textContent=t;if($('#mediaStatus'))$('#mediaStatus').textContent=t}
function tc(v){const p=String(v).trim().replace(',','.').split(':').map(Number);return Math.round((p.length===3?p[0]*3600+p[1]*60+p[2]:p[0]*60+p[1])*1000)}
function ft(v){if(!Number.isFinite(v))return'0:00';const m=Math.floor(v/60),s=Math.floor(v%60);return`${m}:${String(s).padStart(2,'0')}`}
function cloneLines(v){return(v||[]).map(l=>({...l,words:l.words?l.words.map(w=>({...w})):null}))}
function openSaveDB(){return new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,1);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains(DB_STORE))r.result.createObjectStore(DB_STORE)};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function saveMedia(key,file){try{const db=await openSaveDB(),tx=db.transaction(DB_STORE,'readwrite');tx.objectStore(DB_STORE).put(file||null,key);await new Promise((r,j)=>{tx.oncomplete=r;tx.onerror=()=>j(tx.error)});db.close()}catch{}}
async function loadMedia(key){try{const db=await openSaveDB(),tx=db.transaction(DB_STORE,'readonly'),req=tx.objectStore(DB_STORE).get(key),v=await new Promise((r,j)=>{req.onsuccess=()=>r(req.result||null);req.onerror=()=>j(req.error)});db.close();return v}catch{return null}}
async function clearSavedMedia(){try{const db=await openSaveDB(),tx=db.transaction(DB_STORE,'readwrite');tx.objectStore(DB_STORE).clear();await new Promise((r,j)=>{tx.oncomplete=r;tx.onerror=()=>j(tx.error)});db.close()}catch{}}
const SAVE_IDS=['autosaveToggle','syncMethod','lyricsText','manualLyricsText','grouping','contextMode','lyricsEntrance','customEntrance','titleInput','artistInput','showTitle','showArtworkIntro','titleDuration','useArtworkBg','fontWeight','textAlign','lineHeight','letterSpacing','size','yPos','textColor','glow','offset','bgFit','cropX','cropY','cropZoom','videoMode','videoStart','videoEnd','dim','blur','aspect','quality','safeToggle'];
function collectControls(){const out={};for(const id of SAVE_IDS){const el=$('#'+id);if(el)out[id]=el.type==='checkbox'?el.checked:el.value}return out}
function applyControls(c={}){for(const[id,v]of Object.entries(c)){const el=$('#'+id);if(!el)continue;if(el.type==='checkbox')el.checked=!!v;else el.value=v}if($('#useArtworkBg2'))$('#useArtworkBg2').checked=!!c.useArtworkBg}
function setSaveState(saved){const b=$('#saveProgressBtn'),t=$('#saveProgressText');if(!b||!t)return;b.classList.toggle('saved',saved);t.textContent=saved?'Saved':'Save progress'}
function markDirty(){if(restoring)return;setSaveState(false);if(!$('#autosaveToggle')?.checked)return;clearTimeout(autosaveTimer);autosaveTimer=setTimeout(()=>saveProgress(false,true),1100)}
async function saveProgress(includeMedia=true,automatic=false){const b=$('#saveProgressBtn'),t=$('#saveProgressText');if(!automatic&&b){b.disabled=true;t.textContent='Saving…'}try{const state={version:2,savedAt:Date.now(),controls:collectControls(),sourceBase:cloneLines(sourceBase),sourceLines:cloneLines(sourceLines),reviewLines:cloneLines(reviewLines),lines:cloneLines(lines),selected,offset,manualIndex,reviewMode,selectedSong,activeStep,visited:[...visitedSteps]};localStorage.setItem(SAVE_KEY,JSON.stringify(state));if(includeMedia)await Promise.all([saveMedia('audio',audioFile),saveMedia('background',manualBgFile)]);setSaveState(true);if(!automatic)status('Project progress saved on this device.')}catch{setSaveState(false);if(!automatic)status('Could not save project progress in this browser.')}finally{if(!automatic&&b)b.disabled=false}}
function scheduleMediaSave(key,file){if($('#autosaveToggle')?.checked)saveMedia(key,file)}
function goStep(i,mark=true){i=clamp(i,0,STEPS.length-1);if(mark)visitedSteps.add(activeStep);activeStep=i;visitedSteps.add(i);$$('.navbtn').forEach((b,n)=>{b.classList.toggle('active',n===i);b.classList.toggle('done',visitedSteps.has(n)&&n!==i)});$$('.tool').forEach(x=>x.classList.toggle('active',x.dataset.panel===STEPS[i]));if($('#stepStatus'))$('#stepStatus').textContent=`Step ${i+1} of ${STEPS.length}`;if($('#prevStep'))$('#prevStep').disabled=i===0;if($('#nextStep'))$('#nextStep').textContent=i===STEPS.length-1?'Review':'Next';markDirty()}
function splitBlocks(raw){const out=[],cur=[];for(const row of raw.split(NL)){if(!row.trim()){if(cur.length)out.push(cur.splice(0).join(NL))}else cur.push(row)}if(cur.length)out.push(cur.join(NL));return out}
function parseTimed(text,name='lyrics.lrc'){const lower=name.toLowerCase(),raw=text.split(CR).join('');if(lower.endsWith('.srt')||lower.endsWith('.vtt')||raw.includes('-->')){const out=[];for(const b of splitBlocks(raw.trim())){const r=b.split(NL),i=r.findIndex(x=>x.includes('-->'));if(i<0)continue;const[a,z]=r[i].split('-->').map(x=>tc(x.trim().split(/\s+/)[0])),copy=r.slice(i+1).join(' ').replace(/<[^>]+>/g,'').trim();if(copy)out.push({text:copy,start:a,duration:Math.max(100,z-a),words:null})}return out.sort((a,b)=>a.start-b.start)}const out=[];for(const row of raw.split(NL)){const tags=[...row.matchAll(/\[(\d+:\d+(?:[.:]\d+)?)\]/g)];if(!tags.length)continue;const clean=row.replace(/\[[^\]]+\]/g,''),wm=[...clean.matchAll(/<(\d+:\d+(?:[.:]\d+)?)>([^<]*)/g)],copy=clean.replace(/<[^>]+>/g,'').trim();for(const t of tags){if(!copy)continue;const start=tc(t[1]),words=wm.length?wm.map(m=>({text:m[2].trim(),start:tc(m[1]),duration:0,emphasis:1,hold:1})).filter(w=>w.text):null;out.push({text:copy,start,duration:2600,words})}}out.sort((a,b)=>a.start-b.start);out.forEach((x,i)=>{x.duration=Math.max(100,(out[i+1]?.start||x.start+2600)-x.start);x.words?.forEach((w,j)=>w.duration=Math.max(60,(x.words[j+1]?.start||x.start+x.duration)-w.start))});return out}
function inferred(line){const raw=line.text.trim().split(/\s+/).filter(Boolean);if(!raw.length)return[];const weights=raw.map(w=>Math.max(1,w.replace(/[^\p{L}\p{N}]/gu,'').length*.72+1.35)),sum=weights.reduce((a,b)=>a+b,0);let cur=line.start;return raw.map((text,i)=>{const d=i===raw.length-1?Math.max(60,line.start+line.duration-cur):Math.max(60,Math.round(line.duration*weights[i]/sum)),o={text,start:cur,duration:d,emphasis:1,hold:1};cur+=d;return o})}
function units(line){return line.words?.length?line.words:inferred(line)}
function suspicious(text,i,n){if(i>2&&i<n-3)return false;return /https?:\/\/|www\.|\.com\b|\.net\b|\.org\b|genius|musixmatch|azlyrics|lyricsfreak|lyrics source|transcribed by|provided by|copyright|all rights reserved|^source\s*:|^title\s*:|^artist\s*:|^album\s*:/.test(text.toLowerCase().trim())}
function startReview(items,mode='timed'){reviewMode=mode;reviewLines=cloneLines(items).map((l,i,a)=>({...l,hidden:false,flagged:suspicious(l.text,i,a.length)}));renderReview();$('#reviewBox')?.classList.remove('hidden');$('#manualTimingBox')?.classList.add('hidden');status(`${reviewLines.length} lyric lines ready to review.`);markDirty()}
function renderReview(){const box=$('#reviewList');if(!box)return;$('#reviewCount').textContent=`${reviewLines.filter(x=>!x.hidden).length} of ${reviewLines.length} shown`;box.innerHTML=reviewLines.map((l,i)=>`<div class="review-row${l.flagged?' flagged':''}${l.hidden?' hidden-line':''}" data-r="${i}"><div class="review-text"><b>${esc(l.text)}</b><small>${l.flagged?'Possible metadata / website line':l.hidden?'Hidden from video':'Lyric line'}</small></div><div class="review-actions"><button data-act="hide">${l.hidden?'Show':'Hide'}</button><button data-act="delete">Delete</button></div></div>`).join('')}
function applyGrouping(announce=true){const mode=$('#grouping')?.value||'original';if(!sourceLines.length){lines=[];timeline();render(audio.currentTime*1000);return}if(mode==='original')lines=cloneLines(sourceLines);else{const n=+mode,out=[];for(const src of sourceLines){const us=units(src);for(let i=0;i<us.length;i+=n){const chunk=us.slice(i,i+n);if(!chunk.length)continue;const start=chunk[0].start,end=Math.max(...chunk.map(w=>w.start+w.duration));out.push({text:chunk.map(w=>w.text).join(' '),start,duration:Math.max(50,end-start),words:chunk.map(w=>({...w}))})}}lines=out.sort((a,b)=>a.start-b.start)}selected=Math.min(selected,Math.max(0,lines.length-1));timeline();render(audio.currentTime*1000);if(announce)status(mode==='original'?`Natural lyric lines · ${lines.length} lines.`:`${mode}-word lyric phrasing applied.`);markDirty()}
function ci(ms){let i=0;for(let n=0;n<lines.length;n++){if(lines[n].start+offset<=ms)i=n;else break}return i}
function entranceMs(){if(!lines.length)return Infinity;const first=Math.max(0,lines[0].start+offset),mode=$('#lyricsEntrance')?.value||'at';if(mode==='before05')return Math.max(0,first-500);if(mode==='before1')return Math.max(0,first-1000);if(mode==='before2')return Math.max(0,first-2000);if(mode==='custom')return Math.max(0,(+$('#customEntrance').value||0)*1000);return first}
function titleWindowMs(){const title=$('#titleInput')?.value.trim();if(!title||!$('#showTitle')?.checked||!lines.length)return 0;const first=Math.max(0,lines[0].start+offset),max=+$('#titleDuration').value*1000;return first<450?0:Math.min(max,Math.max(0,first-180))}
function updateIntro(ms){const ent=entranceMs(),showLyrics=ms>=ent&&lines.length>0;lyricsEl.classList.toggle('visible',showLyrics);lyricsEl.classList.toggle('preenter',!showLyrics);const tw=titleWindowMs(),title=$('#titleInput')?.value.trim()||'',showTitle=!!title&&tw>0&&ms<tw&&ms<ent;$('#titleCard')?.classList.toggle('on',showTitle);if($('#titleName'))$('#titleName').textContent=title;if($('#titleArtist'))$('#titleArtist').textContent=$('#artistInput')?.value.trim()||'';if($('#titleAlbum'))$('#titleAlbum').textContent=selectedSong?.collectionName||'';$('#introArt')?.classList.toggle('on',showTitle&&$('#showArtworkIntro')?.checked&&!!artworkObjectURL)}
function wordTimingEmphasis(line,w){const explicit=clamp(Number(w.emphasis)||1,.7,2),us=line.words?.length?line.words:null;if(!us||us.length<2)return explicit;const ds=us.map(x=>Math.max(60,x.duration||0)).sort((a,b)=>a-b),median=ds[Math.floor(ds.length/2)]||w.duration||1,stretch=clamp((w.duration||median)/Math.max(1,median),.75,1.9);return clamp(explicit*(1+Math.max(0,stretch-1)*.26),.7,2.15)}
function wordMotion(line,w,ms){const hold=Math.max(.25,w.hold||1),duration=Math.max(50,w.duration*hold),raw=clamp((ms-(w.start+offset))/duration,0,1),progress=smooth(raw),pulse=Math.sin(Math.PI*raw),strength=wordTimingEmphasis(line,w),lift=.016+Math.max(0,strength-1)*.065;return{raw,progress,pulse,strength,scale:1+pulse*lift,bright:1+pulse*(.09+Math.max(0,strength-1)*.16),glow:1+pulse*(.20+Math.max(0,strength-1)*.42)}}
function wordMarkup(line,ms){return units(line).map(w=>{const m=wordMotion(line,w,ms),fill=(m.progress*100).toFixed(1);return`<span class="apple-word" style="--word-scale:${m.scale.toFixed(4)}"><span class="base">${esc(w.text)}</span><span class="fill" style="--fill:${fill}%">${esc(w.text)}</span></span>`}).join(' ')}
function transitionProgress(ms,i){const next=lines[i+1];if(!next)return 0;const nextStart=next.start+offset,lead=Math.min(700,Math.max(360,(nextStart-(lines[i].start+offset))*.24));return smooth((ms-(nextStart-lead))/lead)}
function contextWindow(){const v=$('#contextMode')?.value||'3';if(v==='current')return{before:0,after:0,total:1};const total=Math.max(3,Math.min(9,+v||3)),before=Math.floor((total-1)/2);return{before,after:total-1-before,total}}
function render(ms){updateIntro(ms);if(!lines.length){lyricsEl.textContent='Add lyrics to begin';if($('#activeMeta'))$('#activeMeta').textContent='No lyrics loaded';return}if(ms<entranceMs()){lyricsEl.innerHTML='';if($('#activeMeta'))$('#activeMeta').textContent='Waiting for lyrics entrance';return}const i=ci(ms),q=transitionProgress(ms,i),cw=contextWindow();selected=i;if($('#activeMeta'))$('#activeMeta').textContent=`Lyric ${i+1} of ${lines.length}`;$$('.line').forEach((x,n)=>x.classList.toggle('active',n===i));const parts=[];for(let idx=Math.max(0,i-cw.before);idx<=Math.min(lines.length-1,i+cw.after);idx++){const rel=idx-i,d=Math.abs(rel);if(rel===0){parts.push(`<div class="apple-line apple-current" style="opacity:${(1-.68*q).toFixed(3)};transform:translateY(${-18*q}px) scale(${(1-.045*q).toFixed(3)})">${wordMarkup(lines[idx],ms)}</div>`);continue}const near=rel===1,baseAlpha=clamp(.42-d*.085,.09,.36),fade=rel<0?baseAlpha*(1-q*.5):baseAlpha+(near?.56*q:.08*q);parts.push(`<div class="apple-line apple-neighbour" style="opacity:${clamp(fade,.04,.92).toFixed(3)}">${near&&rel>0?wordMarkup(lines[idx],ms):esc(lines[idx].text)}</div>`)}lyricsEl.innerHTML=`<div class="apple-flow">${parts.join('')}</div>`}
function timeline(){const b=$('#timeline');if(!b)return;if(!lines.length){b.innerHTML='<div class="statusbox">Your synced lyric timeline will appear here.</div>';return}b.innerHTML=lines.map((l,i)=>`<button class="line" data-i="${i}"><time>${(l.start/1000).toFixed(2)}</time><b>${esc(l.text)}</b></button>`).join('')}
function fillEditor(i){const l=lines[i];if(!l)return;if($('#currentText'))$('#currentText').value=l.text;if($('#currentStart'))$('#currentStart').value=(l.start/1000).toFixed(2);if($('#currentDuration'))$('#currentDuration').value=(l.duration/1000).toFixed(2);renderWordEditor(l)}
function renderWordEditor(l){const box=$('#wordEditor');if(!box)return;box.innerHTML=units(l).map((w,i)=>`<div class="word-row" data-w="${i}"><button type="button">${esc(w.text)}</button><input class="emph" type="number" min="0.7" max="2" step="0.1" value="${(w.emphasis||1).toFixed(1)}"><input class="hold" type="number" min="0.25" max="4" step="0.25" value="${(w.hold||1).toFixed(2)}"></div>`).join('')}
function look(){lyricsEl.style.fontSize=($('#size')?.value||52)+'px';lyricsEl.style.top=($('#yPos')?.value||50)+'%';lyricsEl.style.textAlign=$('#textAlign')?.value||'left';lyricsEl.style.fontWeight=$('#fontWeight')?.value||700;lyricsEl.style.letterSpacing=($('#letterSpacing')?.value||-.02)+'em';document.documentElement.style.setProperty('--lyric-weight',$('#fontWeight')?.value||700);document.documentElement.style.setProperty('--lyric-lh',$('#lineHeight')?.value||1.02);document.documentElement.style.setProperty('--accent',$('#textColor')?.value||'#fff');if($('#sizeVal'))$('#sizeVal').textContent=$('#size')?.value||52;if($('#glowVal'))$('#glowVal').textContent=($('#glow')?.value||100)+'%';if($('#offsetVal'))$('#offsetVal').textContent=`${offset>0?'+':''}${offset} ms`;if($('#lineHeightVal'))$('#lineHeightVal').textContent=$('#lineHeight')?.value||1.02;if($('#letterSpacingVal'))$('#letterSpacingVal').textContent=(+($('#letterSpacing')?.value||-.02)).toFixed(2)+'em';render(audio.currentTime*1000)}