# KEFE Visual Effects

Production effects are maintained as independent modules under this directory. Each module registers itself on `window.kefeEffects` and receives the common renderer arguments `(ctx, width, height, style, lines, time)`.

The effect registry is intentionally separate from the DeepSeek development archive.
